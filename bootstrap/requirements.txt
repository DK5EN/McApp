# MCProxy Python dependencies
# Minimum versions for compatibility - uv will resolve latest compatible versions
#
# Core networking
websockets>=14.0          # WebSocket server for client connections
httpx>=0.28.0             # Modern async HTTP client (replaces requests)

# Bluetooth Low Energy
dbus-next>=0.2.3          # D-Bus interface for BlueZ BLE

# Utilities
timezonefinder>=6.5.0     # Timezone detection for node time sync
zstandard>=0.23.0         # Compression support

# Note: These are minimum versions, not pinned.
# Security updates flow through automatically.
# uv resolves the latest compatible versions at install time.
