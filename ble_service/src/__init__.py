# BLE Service for MCProxy
"""
Standalone BLE service that exposes Bluetooth Low Energy functionality
via HTTP REST API and Server-Sent Events (SSE).

This service runs on hardware with Bluetooth support (e.g., Raspberry Pi)
and allows remote clients to interact with BLE devices.
"""

__version__ = "0.1.0"
