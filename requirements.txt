# MCProxy Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (existing)
websockets>=14.0
dbus-next>=0.2.3
timezonefinder>=6.5.0

# HTTP client (replaces requests for async support)
httpx>=0.28.0
aiohttp>=3.9.0
aiohttp-sse-client>=0.2.1

# SSE/FastAPI support (optional - for Server-Sent Events transport)
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0
sse-starlette>=2.0.0

# Note: sqlite3 is built into Python 3.11+
# Note: asyncio is built into Python 3.11+

# Development dependencies (optional)
# ruff>=0.8.0  # Linting
# pytest>=8.0  # Testing
# pytest-asyncio>=0.24.0  # Async test support
